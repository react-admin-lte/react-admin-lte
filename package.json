{
  "private": true,
  "devDependencies": {
    "codecov": "^2.1.0",
    "lerna": "^2.0.0-rc.4"
  },
  "scripts": {
    "build": "lerna run build",
    "postinstall": "lerna bootstrap --concurrency=1",
    "test": "lerna run test"
  }
}
