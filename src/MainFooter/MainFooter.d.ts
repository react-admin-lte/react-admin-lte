import * as React from 'react';

export interface MainFooterProps {
  component?: React.ReactType<MainFooterProps>;
}

declare const MainFooter: React.ComponentType<MainFooterProps>;

export default MainFooter;
