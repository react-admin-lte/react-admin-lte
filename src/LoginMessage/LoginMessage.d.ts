import * as React from 'react';

export interface LoginMessageProps {
  component?: React.ReactType<LoginMessageProps>;
}

declare const LoginMessage: React.ComponentType<LoginMessageProps>;

export default LoginMessage;
